#include "main.h"

/**
  *_isdigit - checks for a number value
  *@c: integer input
  *
  *Return: 0
  */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}

